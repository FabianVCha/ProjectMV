---
apiVersion: v1
kind: Service
metadata:
  name: openclaw-pod-a
  labels:
    app: openclaw-pod-a
spec:
  selector:
    app: openclaw-pod-a
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: openclaw-pod-b
  labels:
    app: openclaw-pod-b
spec:
  selector:
    app: openclaw-pod-b
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
  type: ClusterIP
