# IMPORTANTE: Este archivo tiene placeholders.
# Los valores reales se inyectan desde GitHub Actions Secrets.
# NUNCA pongas tokens o API keys reales aquí.
---
apiVersion: v1
kind: Secret
metadata:
  name: openclaw-pod-a-secrets
  namespace: andres-vim
type: Opaque
stringData:
  OPENCLAW_GATEWAY_TOKEN: "${ANDRES_POD_A_GATEWAY_TOKEN}"
  GEMINI_API_KEY: "${ANDRES_GEMINI_API_KEY}"
  ANTHROPIC_API_KEY: "${ANDRES_ANTHROPIC_API_KEY}"
  OPENAI_API_KEY: "${ANDRES_OPENAI_API_KEY}"
  OPENROUTER_API_KEY: "${ANDRES_OPENROUTER_API_KEY}"

---
apiVersion: v1
kind: Secret
metadata:
  name: openclaw-pod-b-secrets
  namespace: andres-vim
type: Opaque
stringData:
  OPENCLAW_GATEWAY_TOKEN: "${ANDRES_POD_B_GATEWAY_TOKEN}"
  GEMINI_API_KEY: "${ANDRES_GEMINI_API_KEY}"
  ANTHROPIC_API_KEY: "${ANDRES_ANTHROPIC_API_KEY}"
  OPENAI_API_KEY: "${ANDRES_OPENAI_API_KEY}"
  OPENROUTER_API_KEY: "${ANDRES_OPENROUTER_API_KEY}"
